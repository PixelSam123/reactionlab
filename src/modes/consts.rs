/// Width below which the start screen content will stack vertically instead of horizontally
pub const START_STACK_WIDTH: f32 = 600.0;
pub const HISTORY_LINE_PLOT_HEIGHT: f32 = 180.0;
/// Giving a "container style" so UI content doesn't go too wide
pub const MAX_CONTENT_WIDTH: f32 = 1000.0;
